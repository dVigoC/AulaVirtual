// ── app/(dashboard)/admin.tsx  ───────────────────────────────────────────────
// El administrador ve la pantalla de inicio completa:
// gestión de portadas, publicaciones y vista de estudiante incluidas.
import InicioModule from "../../components/inicio/InicioScreen";

export default function AdminHome() {
  return <InicioModule />;
}
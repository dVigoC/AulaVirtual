// ── app/(dashboard)/docente.tsx  ─────────────────────────────────────────────
// El docente ve sus cursos asignados, puede subir portada y publicar contenido.
import InicioModule from "../../components/inicio/InicioScreen";

export default function DocenteHome() {
  return <InicioModule />;
}